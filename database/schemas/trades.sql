CREATE TABLE IF NOT EXISTS trades (id BIGSERIAL PRIMARY KEY, epic TEXT, direction TEXT, size DOUBLE PRECISION, entry_price DOUBLE PRECISION, exit_price DOUBLE PRECISION, pnl DOUBLE PRECISION, opened_at TIMESTAMPTZ, closed_at TIMESTAMPTZ);
