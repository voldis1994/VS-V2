CREATE TABLE IF NOT EXISTS candles (id BIGSERIAL PRIMARY KEY, instrument_id INT, bucket_start TIMESTAMPTZ, open DOUBLE PRECISION, high DOUBLE PRECISION, low DOUBLE PRECISION, close DOUBLE PRECISION, tick_count INT);
